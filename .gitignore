# Environment variables — NEVER commit these
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# FAISS generated index files (local dev only, re-generated by ingest.py)
backend/vectorstore/

# SQLite database (local dev only)
backend/history.db
backend/*.db

# Uploaded documents (user-uploaded at runtime, not part of the repo)
# The pre-seeded documents/ folder IS committed so the app works out of the box
# but user-uploaded files should not be
backend/documents/sap.pdf
backend/documents/RESUME*.pdf

# Hypothesis test data
backend/.hypothesis/
.hypothesis/

# Node / React
node_modules/
frontend/dist/
frontend/build/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
