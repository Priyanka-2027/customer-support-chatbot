# Web framework
fastapi==0.111.0
uvicorn==0.30.1

# Required for multipart/form-data file uploads (POST /upload)
python-multipart==0.0.9

# LangChain core and community integrations
langchain==0.2.6
langchain-community==0.2.6
langchain-google-genai==1.0.7

# PDF loading
pypdf==4.2.0

# Embeddings (local, free)
sentence-transformers==3.3.1
langchain-huggingface==0.0.3

# Vector store
faiss-cpu==1.14.3

# Pinecone cloud vector store (production backend)
langchain-pinecone==0.2.13

# Environment variable management
python-dotenv==1.0.1

# HTTP client (used internally by LangChain)
httpx==0.27.0

# Numerical operations
numpy==1.26.4

# Async SQLite — chat history persistence
aiosqlite==0.20.0

# JWT authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# google-genai SDK (used directly for Gemini API calls)
google-genai>=0.8.0

# grpc + protobuf (required by google-ai SDK)
grpcio>=1.60.0
protobuf>=5.0.0
